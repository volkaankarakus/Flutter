// An interface is a construct that we add properties to our classes.
// We use ready-made interfaces.
// We can create interfaces ourselves.
// We can add more than one interface to class.

abstract class Interface1{
  late int variable ;
  void method1();
  String method2(); 
}